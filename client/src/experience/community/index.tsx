import type { ScreenComponent } from "../data/types";
import { ScreenPlaceholder } from "../shared/ScreenPlaceholder";

/** Screens 25-27 — built in Phase 12. */
export const communityScreens: Record<number, ScreenComponent> = {
  25: ScreenPlaceholder,
  26: ScreenPlaceholder,
  27: ScreenPlaceholder,
};
