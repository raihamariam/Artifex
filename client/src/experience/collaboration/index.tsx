import type { ScreenComponent } from "../data/types";
import { ScreenPlaceholder } from "../shared/ScreenPlaceholder";

/** Screens 05-07 — built in Phase 5. */
export const collaborationScreens: Record<number, ScreenComponent> = {
  5: ScreenPlaceholder,
  6: ScreenPlaceholder,
  7: ScreenPlaceholder,
};
