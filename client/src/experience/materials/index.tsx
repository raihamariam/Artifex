import type { ScreenComponent } from "../data/types";
import { ScreenPlaceholder } from "../shared/ScreenPlaceholder";

/** Screens 15-17 — built in Phase 8. */
export const materialsScreens: Record<number, ScreenComponent> = {
  15: ScreenPlaceholder,
  16: ScreenPlaceholder,
  17: ScreenPlaceholder,
};
