import type { ScreenComponent } from "../data/types";
import { ScreenPlaceholder } from "../shared/ScreenPlaceholder";

/** Screens 23-24 — built in Phase 11. */
export const releaseScreens: Record<number, ScreenComponent> = {
  23: ScreenPlaceholder,
  24: ScreenPlaceholder,
};
